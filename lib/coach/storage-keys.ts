/** Browser localStorage keys for coach UX (no secrets). */
export const COACH_STORAGE_KEYS = {
  tone: 'glowflow.coach.tone.v1',
  weeklyGoals: 'glowflow.coach.weeklyGoals.v1',
  journal: 'glowflow.coach.journal.v1',
  visits: 'glowflow.coach.visits.v1',
} as const;
